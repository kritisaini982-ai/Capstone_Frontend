import React, { useState } from 'react';
import { 
  ShieldAlert, ShieldCheck, AlertTriangle, FileCode, 
  Upload, Terminal, ExternalLink, CheckCircle, XCircle, Clock 
} from 'lucide-react';

export default function ObservatoryDashboard() {
  const [selectedTab, setSelectedTab] = useState('diff');
  const [status, setStatus] = useState('FLAGGED');

  return (
    <div className="min-h-screen bg-slate-950 text-slate-100 font-sans p-6">
      {/* Header */}
      <header className="flex justify-between items-center pb-6 border-b border-slate-800">
        <div>
          <h1 className="text-2xl font-bold tracking-tight text-white flex items-center gap-2">
            <ShieldAlert className="text-cyan-400" /> 
            Browser Extension Risk Observatory
          </h1>
          <p className="text-sm text-slate-400">Supply-Chain Risk & Permission Analysis Engine</p>
        </div>
        <div className="flex gap-3">
          <button className="flex items-center gap-2 bg-cyan-600 hover:bg-cyan-500 text-white px-4 py-2 rounded-lg text-sm font-medium transition">
            <Upload size={16} /> Upload Package (.zip/.crx)
          </button>
        </div>
      </header>

      {/* Metrics Row */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-4 my-6">
        <MetricCard title="Extensions Scanned" value="128" sub="Total packages in DB" icon={<FileCode className="text-blue-400" />} />
        <MetricCard title="High Risk Detected" value="14" sub="Score > 75/100" icon={<AlertTriangle className="text-amber-400" />} />
        <MetricCard title="Avg Scan Time" value="1.8s" sub="Syft + Grype processing" icon={<Clock className="text-emerald-400" />} />
        <MetricCard title="Allowlisted" value="89" sub="Passed policy check" icon={<ShieldCheck className="text-cyan-400" />} />
      </div>

      {/* Main Analysis View */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        
        {/* Left Column: Extension Detail & Risk Panel */}
        <div className="lg:col-span-1 bg-slate-900 border border-slate-800 rounded-xl p-5 space-y-6">
          <div>
            <div className="flex justify-between items-start">
              <div>
                <h2 className="text-lg font-bold text-white">AdBlocker Ultra Pro</h2>
                <span className="text-xs text-slate-400">ID: chrome-ext-9082</span>
              </div>
              <span className={`px-2.5 py-1 rounded-full text-xs font-semibold ${
                status === 'ALLOWLISTED' ? 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20' :
                status === 'REJECTED' ? 'bg-rose-500/10 text-rose-400 border border-rose-500/20' :
                'bg-amber-500/10 text-amber-400 border border-amber-500/20'
              }`}>
                {status}
              </span>
            </div>
          </div>

          {/* Risk Score Gauge */}
          <div className="bg-slate-950 p-4 rounded-lg border border-slate-800 text-center">
            <span className="text-xs uppercase tracking-wider text-slate-400">Composite Risk Score</span>
            <div className="text-4xl font-extrabold text-amber-400 my-1">82 / 100</div>
            <p className="text-xs text-rose-400 font-medium">High Risk: Over-privileged & version drift detected</p>
          </div>

          {/* Scan Findings Breakdown */}
          <div className="space-y-3">
            <h3 className="text-sm font-semibold text-slate-300">Risk Contributors</h3>
            
            <FindingItem label="High Severity Permissions" detail="<all_urls>, webRequest, cookies" points="+40" color="text-rose-400" />
            <FindingItem label="Vulnerable NPM Dependencies" detail="1 Critical CVE found via Grype" points="+25" color="text-amber-400" />
            <FindingItem label="Unregistered Remote Domains" detail="2 external endpoints found in AST" points="+17" color="text-amber-400" />
          </div>

          {/* Exception & Decision Workflow */}
          <div className="pt-4 border-t border-slate-800 space-y-3">
            <h3 className="text-sm font-semibold text-slate-300">Administrative Decision</h3>
            <div className="grid grid-cols-2 gap-2">
              <button 
                onClick={() => setStatus('ALLOWLISTED')}
                className="flex items-center justify-center gap-1.5 bg-emerald-600/20 hover:bg-emerald-600/30 text-emerald-400 border border-emerald-600/30 py-2 rounded-lg text-xs font-medium transition">
                <CheckCircle size={14} /> Allowlist
              </button>
              <button 
                onClick={() => setStatus('REJECTED')}
                className="flex items-center justify-center gap-1.5 bg-rose-600/20 hover:bg-rose-600/30 text-rose-400 border border-rose-600/30 py-2 rounded-lg text-xs font-medium transition">
                <XCircle size={14} /> Reject Extension
              </button>
            </div>
          </div>
        </div>

        {/* Right Column: Version Diff & In-Depth Inspection */}
        <div className="lg:col-span-2 bg-slate-900 border border-slate-800 rounded-xl p-5 flex flex-col">
          
          {/* Navigation Tabs */}
          <div className="flex border-b border-slate-800 mb-4 gap-6">
            <TabButton active={selectedTab === 'diff'} onClick={() => setSelectedTab('diff')} label="Version-to-Version Diff" />
            <TabButton active={selectedTab === 'permissions'} onClick={() => setSelectedTab('permissions')} label="Least-Privilege Analysis" />
            <TabButton active={selectedTab === 'sbom'} onClick={() => setSelectedTab('sbom')} label="SBOM & Grype Vulnerabilities" />
          </div>

          {/* Tab Content: Version Diffing */}
          {selectedTab === 'diff' && (
            <div className="space-y-4 flex-1">
              <div className="flex justify-between items-center text-xs text-slate-400 bg-slate-950 p-3 rounded-lg border border-slate-800">
                <span>Comparing Baseline: <strong className="text-slate-200">v1.0.0</strong></span>
                <span>Target Version: <strong className="text-cyan-400">v1.1.0 (Uploaded)</strong></span>
              </div>

              {/* Code/Manifest Diff Code Block */}
              <div className="bg-slate-950 rounded-lg p-4 font-mono text-xs overflow-x-auto border border-slate-800 space-y-1">
                <div className="text-slate-500">// manifest.json permission drift analysis</div>
                <div className="text-slate-300">  &quot;permissions&quot;: [</div>
                <div className="text-slate-400">    &quot;storage&quot;,</div>
                <div className="text-slate-400">    &quot;activeTab&quot;,</div>
                <div className="text-emerald-400 bg-emerald-950/40 p-0.5 rounded">+   &quot;cookies&quot;,</div>
                <div className="text-emerald-400 bg-emerald-950/40 p-0.5 rounded">+   &quot;webRequest&quot;</div>
                <div className="text-slate-300">  ],</div>
                <div className="text-slate-500">// background.js extracted remote endpoints</div>
                <div className="text-rose-400 bg-rose-950/40 p-0.5 rounded">+  {fetch("https://analytics-telemetry-unknown.com/collect", &#123; method: "POST" &#125;);}</div>
              </div>

              <div className="bg-amber-500/10 border border-amber-500/20 text-amber-300 text-xs p-3 rounded-lg flex items-start gap-2">
                <AlertTriangle size={16} className="shrink-0 mt-0.5" />
                <span><strong>Security Alert:</strong> Version 1.1.0 requested high-privilege APIs (cookies, webRequest) and added an unverified external communication endpoint not present in Version 1.0.0.</span>
              </div>
            </div>
          )}

          {/* Tab Content Placeholder */}
          {selectedTab !== 'diff' && (
            <div className="flex-1 flex items-center justify-center text-slate-500 text-sm italic">
              Data view loaded via Spring Boot REST API endpoint: /api/v1/extensions/ext-9082/{selectedTab}
            </div>
          )}

        </div>
      </div>
    </div>
  );
}

// Helper Components
function MetricCard({ title, value, sub, icon }) {
  return (
    <div className="bg-slate-900 border border-slate-800 rounded-xl p-4 flex justify-between items-start">
      <div>
        <p className="text-xs font-medium text-slate-400">{title}</p>
        <p className="text-2xl font-bold text-white my-0.5">{value}</p>
        <p className="text-[11px] text-slate-500">{sub}</p>
      </div>
      <div className="bg-slate-950 p-2.5 rounded-lg border border-slate-800">{icon}</div>
    </div>
  );
}

function FindingItem({ label, detail, points, color }) {
  return (
    <div className="flex justify-between items-center text-xs p-2.5 bg-slate-950 rounded-lg border border-slate-800/80">
      <div>
        <div className="text-slate-200 font-medium">{label}</div>
        <div className="text-slate-400 text-[11px]">{detail}</div>
      </div>
      <span className={font-mono font-bold ${color}}>{points}</span>
    </div>
  );
}

function TabButton({ active, onClick, label }) {
  return (
    <button 
      onClick={onClick} 
      className={`pb-3 text-xs font-medium transition border-b-2 ${
        active ? 'border-cyan-400 text-cyan-400' : 'border-transparent text-slate-400 hover:text-slate-200'
      }`}>
      {label}
    </button>
  );
}